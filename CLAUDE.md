# どうぶつベースボール — 開発メモ

外部ライブラリなしの単一HTMLで動く3D野球ゲーム。WebGL 1.0を自前で叩いている。

この文書は**触る前に読む規則**。いまどこまで進んでいて次に何をするかは
[HANDOFF.md](HANDOFF.md)（引き継ぎ書）にある。

---

## このゲームが目指すもの（2026-09-10 決定）

**コンセプト：かわいいけど、ちょっと怖い。なんかウケル。**

**ルールは厳密、世界とキャラは奇想天外。** この2つは矛盾しない。
**ルールがツッコミ、キャラと世界がボケ**という関係になっている。
おかしなことが起きたとき、審判が規則にのっとって淡々と裁定を下す
——その「誰もツッコまない厳密さ」が笑いの装置。ルールを緩めた瞬間に
ただのバカゲーになり、「なんかウケル」が死ぬ。厳密さは制約ではなくエンジン。

- **野球のルール**：とにかく厳密。野球で起きることはすべて起きうる
  （アピールプレイ、インフィールドフライ、振り逃げ、妨害、ボークなど）。
- **動物の動きとフィールド**：厳密さは不要。人間の野球では起きないことが起きる。

**「怖い」はキャラクターが担当する。** 世界（球場のハザード）ではない。
現実の動物どうしの関係が野球場でそのまま発動する——クマがウサギを食べる、など。

⚠️ **捕食はまだ実装しない**（2026-09-10 時点）。先にキャラクターを磨き込む。

実装するときに決めること：
- 捕食が起きたときの**裁定**。野球規則に該当条文はないので、それらしい条文を
  でっち上げて淡々と適用する（走塁妨害で進塁を認める、など）。ここが笑いの核心。
- 食べられた選手は**戻ってくるのか**。戻らないなら守備が8人になる。
- **頻度**と、プレイヤーが予測できるか。
- **捕食関係の表**（誰が誰を食べるか）。パンダが誰も襲わない、
  カバが見境なく暴れる、といった配置自体が笑いになる。

**絵は最後までかわいいまま。** 怖いのは造形ではなく出来事。丸くてかわいい顔のまま、
平然と恐ろしいことをするほうが怖いし面白い。グロテスクにすると
お子さんの参考イラストという最大の資産と喧嘩する。

**未決定**：プレイヤーに走塁を操作させるか。いまの操作は打撃と投球だけで、
ハザードが起きても見ているだけになっている。

```bash
sh build.sh                                  # src/ を連結して animal-baseball.html を生成
cat src/*.js > /tmp/all.js && node --check /tmp/all.js   # 構文チェック
```

`animal-baseball.html` は**生成物**。srcを1行変えれば全体が変わるので、
並行ブランチでは必ず衝突する。衝突したら解決せず再生成すること。

```bash
git checkout --ours animal-baseball.html && sh build.sh && git add animal-baseball.html
```

| ファイル | 役割 |
|---|---|
| `src/00-shell.html` | HTML外枠・CSS・HUD/メニューのマークアップ |
| `src/10-core.js` | 行列、プリミティブ生成、WebGLレンダラ、効果音 |
| `src/20-world.js` | フィールドのライン、5球場、車・買い物客 |
| `src/30-actors.js` | 動物キャラの造形と描画、チームデータ |
| `src/40-game.js` | 野球ルール、打球の物理、守備判定、走塁 |
| `src/50-main.js` | 画面遷移、入力、カメラ、毎フレームの更新と描画 |

`patches/` は開発履歴。ビルドには不要。
`tools/` は開発ツール（顔ラボ・造形ラボ・メッシュ焼き・ヘッドレス撮影）。
ゲームのビルドには不要。
`src/15-meshes.js` は **`tools/mesh2js.py` の生成物**。手で編集せず、`python tools/mesh2js.py` で焼き直す。

---

## 不変条件（触る前に必ず読む）

いずれも**過去に一度間違えて、時間を溶かした**もの。推測で書き換えないこと。

### 座標系

- 本塁の頂点が原点、`+z` がセンター方向。
- **`+x` は三塁側（レフト方向）**。一塁は `-x` にある（`BASE_POS[0] = [-19.4, 19.4]`）。
- **画面の右方向はワールド `-x`。** カメラが右手系なので、+zを向いたとき+xは左に写る。
  一塁を`-x`に置いてあるのはそのため。ここを逆にするとダイヤモンドが鏡像になる。
- `polar(a, d) = [sin(a)*d, cos(a)*d]`。`a=0`がセンター、`+a`がレフト方向。
- 打球方向 `dir` も同じ規約。右打者の**引っ張り＝`+dir`（レフト方向）**なので、
  早いスイング（`dt<0`）では `dir = -dt*330` と符号が反転する。

### キャラクターの左右

- キャラの正面は local `+z`、上が `+y`。右手系なので **right = forward×up = local `-x`**。
- つまり **local `+x` が左半身、local `-x` が右半身**。
- コード上の名前と実体が逆になっている点に注意：
  - `pose.handL` / `a.hl` → local `-x` = **右腕**
  - `pose.handR` / `a.hr` → local `+x` = **左腕**
- **グローブは左手＝`handR`/`a.hr`。投球は右手＝`handL`。** 一度取り違えている。
- 検証方法：右打者は左肩が投手側を向く。打者の `ry` で local `+x` が投手側になっていればOK。

### プリミティブの巻き順

- 三角形は**外から見て反時計回り（外向き法線）**で定義する。
- `sphere` `dome` `rbox` `cyl` `cone` `taper` は当初内向きに巻かれており、
  裏面（奥側）を描画していた。法線は外向きに与えていたので陰影がそれらしく見え、
  **輪郭線パスを足すまで誰も気づかなかった**。深度も奥側の値になっていた。
- `disc` の巻きが逆だった時期は、内野の土・マウンド・**全キャラの接地影**が
  まるごと描画されていなかった。新しいプリミティブを足したら影と深度を必ず確認。

### シェーダとプリミティブの分割数

- **輪郭線の太さはスクリーン空間で一定**。法線方向への押し出し量（メートル）を
  カメラ距離に比例させている（`uCam` からの距離 / 11.0、`0.30〜3.6` にクランプ）。
  固定値だった頃は、打者（10m）と右翼手（100m）で線の太さが10倍違い、
  さらに `A.ink`（0.022m）より小さいパーツは輪郭に飲まれて真っ黒になった。
  クランプの下限は近景がヘアラインになるのを、上限は遠景が黒い塊になるのを防ぐ。
- **同じ名前のuniformを頂点・フラグメント両方で宣言すると精度が食い違ってリンクに失敗する。**
  フラグメント側は `precision mediump float;`、頂点側は既定で highp。
  `uEye` を両方に書いてリンクが黙って落ち、画面が背景色だけになった。
  頂点側には別名（`uCam`）を用意すること。`R.init` はリンク結果を検査して落ちる。
- 分割数は sphere/rbox が 16×24、dome 9×24、tube 24、disc 32。
  上げてもドローコールは増えないので描画時間はほぼ変わらない（1フレーム3〜4ms）。
  インデックスが `UNSIGNED_SHORT` なので**全プリミティブ合計で頂点65,536個が上限**。
  現在12,476個（うち約10,400がメッシュ）。

### メッシュを焼き込む（段階3）

- 球や箱を積んでも作れない形——鮭の紡錘形、カブトムシの二又に分かれる角——は
  `tools/mesh2js.py` で焼く。`python tools/mesh2js.py` → `src/15-meshes.js`。
- 形は**断面を並べて張る（loft）／曲線に沿って管を通す（sweep）**で作る。
  `tools/obj/*.obj` を置けば、同名の手描きモデル（Blockbench等）が優先される。
- 保存形式は**位置Int16・法線Int8・インデックスUint16をbase64**。
  ランタイムの `geoMesh()` が既存の頂点バッファに流し込むので、
  `part(f, 'sal_body', ...)` と球とまったく同じ書き方で描ける。**スケール1が原寸**。
- **UVも焼ける**（Uint16、`geoMesh` の第7引数）。`Mesh.vert(x,y,z,u,v)` で
  付けたメッシュだけが持つ。これがあるので**焼いた頭に顔のアトラスを貼れる**。

**ハマった点** パッチを**法線方向にオフセットすること**。半径方向ではいけない。
`bear_muz` を体表から6mm浮かせたのに頭が食い込んで穴が空いた。マズルの縁では
半径方向が**表面にほぼ沿って**いるので、そちらに動かしても表面から離れない。
`lifted()` がパラメータ表示から法線を数値微分して出している。

**もっとハマった点** 隆起（マズル・眉・頬）の減衰に `max(0, 1-q) ** p` を
使ってはいけない。見た目は同じだが**縁で曲率が発散する**ので、そこに
オフセットでは抜けられない溝ができる。`bump()` の smoothstep を使うこと。

**ハマった点** 目や斑点を手で座標指定するとメッシュ表面から浮く。
**焼く側で断面プロファイルから解く**こと（`surface(t)`）。

**もっとハマった点** `A.body` を持つ種を哺乳類の顔コードに通してはいけない。
`FACES` に登録がないと `noFaceEyes` の判定をすり抜け、**哺乳類の白い目が
頭スロット（y+1.44）に描かれる**。鮭の頭上に目玉が浮いていた原因はこれ。
`if (FC || A.body)` で止めてある。

### 独自の体を持つ種

- `A.body` を持つ種（`fish` / `beetle`）は脚・腕・胴・頭をすべて自前で描く。
  共有リグのアンカー（胴 y+0.74、頭 y+1.34）は残すので、影・カメラ・
  転倒・スライディングはそのまま効く。
- **鮭は手足なし・バットなし・グローブなし・帽子なし**
  （`noBat` / `noGlove` / `noHat`）。**立たない。** 横倒し（約58度）で
  ビチビチ跳ねているのが通常状態（`fishFlop`）。打席では体ごと水平に
  すっ飛んでいってぶつかる（`fishSwing`）。
  **カブトムシは6本足**で、角で打つ。
- **カブトムシの脚は6本とも「付け根の座標をベイカーで解いて」から吊る。**
  `mesh2js.py` の `bee_shell_x()` が上翅のプロファイルから体表のxを解き、
  `BEE_LEG`（`15-meshes.js` の末尾に焼かれる表）として出す。
  目分量で置いていた頃は6本ともシルエットの内側から生えていた。
  **外に出すと胴から浮いたのは、基節（coxa）の玉がなかったから。**
  いまは 基節→腿→膝→脛→踝→跗節 の6パーツで、
  **3本の骨の長さは付け根の高さ `ly` から毎フレーム解く**ので、
  プロファイルを触って付け根が動いても足は地面に着く。
  前脚だけは焼いた鋸歯つきの脛（`bee_tibL` / `bee_tibR`）を使う。
  これらは**膝から -Y に向けて原寸で焼いてある**ので、`limbTip()` が返す
  関節位置に同じ `rx` / `rz` で置けば `limb()` の円柱と同じ場所に乗る。
- **角はカブトムシの identity。** 遠くからその個体を名指しできる唯一の部分なので、
  太さと長さを削らないこと。頭角は二又×二又で先端4本、胸角も二又。
  細くすると輪郭線（`A.ink`）に飲まれて小枝になる。
- **前胸は上翅より細くする。** 逆にすると頭・前胸・上翅が一つの茶色い塊になる。
  三段が三段に見えるかどうかは幅の差と、値（明度）の差の両方で決まる。
- 鮭の回転は**体の途中（`SAL_PIVOT`）を支点**にする。尻尾を支点にすると
  少し傾けただけで鼻先が1m以上動く。`by` / `bz` で支点位置を打ち消している。
- `G.batSwingT` は**スイング後も何秒も増え続ける**。`fishSwing` は
  「ちょうど1回転して元の姿勢に戻る」ように作ってあり（ピッチが `+2π`、
  他のチャンネルは全部0に戻る）、`SWING_LAG + 0.66` を過ぎたら通常の跳ねに戻す。

- 帽子とヘルメットは哺乳類の頭に合わせたサイズなので `capS` で縮める
  （鮭0.34・カブト0.40）。**耳あてとツバも含めて全パーツに掛けること**。
  1つ掛け忘れると、そこだけ元のサイズで残って巨大な塊になる。
  `capY` は帽子の底が頭（カブトムシは前胸の天面 y+1.26）に接する値にする。
  浮いていると、角のある種ではとくに塊が2つ並んで見える。
- **ユニフォームや模様を球で被せてはいけない。** 体が細いので、球は覆わずに
  ボコッとはみ出す。`bee_band` `sal_band` のように**殻の輪切りをメッシュで焼く**。
  前胸の襟（`bee_cband`）も同じ理由で前胸自身のプロファイルから焼く。
  **色の帯は増やしすぎない。** カブトムシに黄色を3本巻いたらスズメバチになった。
  いまは上翅のサドルだけがユニフォーム色で、残りは縁取り。
- **チーム色は動物自身の色と必ずずらす。** 銀色の鮭に白いユニフォーム、
  茶色の甲虫にオリーブでは遠くから見分けがつかない。
  鮭は濃い青、カブトムシは山吹色にしてある。
- **表情を持たせていない。** 丸くて笑う哺乳類の隣で無表情なほうが不気味で、
  コンセプトに合う。

**ハマった点** `sweep()` の管は当初「x軸を体の横に固定」した枠で張っていた。
進行方向が横を向く枝ではこれが直交せず、管がねじれる。細い角では見えなかったが、
太くした瞬間に出た。いまは進行方向に直交させてから張っている。

**ハマった点** `30-actors.js` から `clock`（`50-main.js`）を参照すると
**顔ラボが壊れる。** `tools/face-lab.html` は `10-core` と `30-actors` しか
連結しないので、`clock` はそこに存在しない。ウサギの耳を揺らすのに使いかけて
気づいた。いまは鮭の `fishFlop` だけが参照していて、ラボが鮭を描かないので
表面化していないだけ。**キャラの動きはポーズ（`p.*`）から作ること。**

**ハマった点** `const SAL_SWING_T = SWING_LAG + 0.66;` を `30-actors.js` の
トップレベルに書いたら、**ゲーム全体が起動しなくなった**。`SWING_LAG` は
`40-game.js`、つまり後から連結されるファイルにあるので、読み込み時点では
まだ初期化されていない（TDZ）。**後のファイルの値をトップレベルで
読んではいけない。** 関数の中でなら実行時に評価されるので問題ない。

### 哺乳類をメッシュ化する（熊）

熊は**共有リグを全部残したまま**殻だけ焼いた最初の哺乳類。腕・脚・帽子・
表情はそのままで、`mesh: 'bear'` が頭・マズル・耳・胴をメッシュに差し替える。
名前は `<mesh>_head` `_face` `_muz` `_snout` `_earin` `_body` `_collar`
`_belt` `_placket` `_stripe` 固定。**他の哺乳類も同じ手順で焼ける。**

- **頭は「(方位角, 仰角) → 点」という1枚の写像**（`bear_head_pt`）。
  これが要。顔のデカールのパッチは**同じ定義域の長方形**なので、
  合わせ込まなくても必ず表面に乗る。マズルは球を貼るのではなく
  この写像に隆起を足して作る——だから頭とマズルが1つの面になる。
- 焼くときは**元のプリミティブの外形（`HEAD_X/Y/Z`）を守る**。
  帽子・ヘルメット・耳の位置はその寸法に合わせて決まっているので、
  変えると帽子だけ浮く。
- **肩の盛り上がりは頭に隠れる高さに置かない。** 胴は62cmしかなく、
  上1/3は頭が覆う。最初 t=0.84 に置いたら完全に頭の中だった。
- **レンズ状の耳は真横から見るとヒレになる。** 2面が鋭い縁で出会うため。
  `sqrt(1 - ρ²)` で縁に垂直に入れると、どの角度からも丸い。
- **模様は胴と同じプロファイルの輪切りで焼く。** 元のピンストライプは
  箱を並べたもので、実は胴に埋まっていた（だから細く見えていた）。
  表面に出すと太く見えるので、幅を取り直すこと。
- `tail: 'nub'` の玉は球の胴に合わせた位置なので、背中の浅い
  メッシュ胴では浮く。`tailZ` で座り直す。
- **袖は胴の外に吊った円柱なので、切り口が肩に平面として露出する。**
  胴のリングを広げても直らない（四角くなるだけ）。肩関節に袖山を1つ被せる。
- **胴の奥行きは幅の8割以上いる。** 3分の2だと横から見て板になり、
  向きを変えてもシルエットが変わらない。いまは9割。

**いちばん高くついた勘違い（2026-09-20）。** 熊に**解剖学を入れてはいけない。**
実物のクマに寄せて、マズルを `mesa`（上面が平らで裾が落ちる＝鼻と額の段差）にし、
肩甲骨の上に盛り上がりを作った。どちらも実物では正しく、**このゲームでは全部
間違いだった**。ここの参照はぬいぐるみであって動物ではない。段差は顔を「鼻面」に
変え、背中の隆起は「背中がどうかしている」に見える。
**丸い顔に柔らかい膨らみ、胴はタマゴ**が正解。`bump` と丸い胴は妥協ではなく決定。

- **背中に盛り上がりを作らない。** 上に出しても後ろに出しても不自然になる。
- **首を作らない。** 頭は胴に直接乗る。胴の上を細く絞ると、その隙間に
  襟（`bear_collar`）のクリーム色が一周して**よだれかけに見える**。
  襟は頭がかぶさる高さ（t 0.93以上）に上げ、胴の天面は広いままにする。
- **見える足の長さを決めるのは裾の高さで、リグではない。** 股関節は
  `y+0.44` 固定（走塁・スライディング・転倒が全部そこに乗っている）なので、
  脚を短くしようとして関節を動かしてはいけない。**胴の底を下げる**と
  そのぶん脚が隠れて短くなる。太さと靴の大きさは `A.legW` / `A.footS`。

**ハマった点** 耳の軸を前向きに寄せる（`ax = (0.42, 0.06, 0.90)`）と、
**真横から板になる**。丸い縁で直したはずの症状が、一段上の階層で再発する。
**45度に開くと正面からも横からも同じだけ丸い。**

**もっとハマった点** 耳の中心を**座標で直接書いてはいけない**。頭を変えた
瞬間に頭蓋の中へ沈み、縁が鎌状に覗くだけになる。カブトムシの脚と同じく、
**頭の写像から付け根を解いて軸方向に持ち上げる**（`EAR_AZ` / `EAR_EL` /
`EAR_LIFT`）。こうしておけば今後頭を触っても耳は頭に乗り続ける。

**ハマった点** 内耳は外耳の表面を**なぞってはいけない**。熊の内耳は外耳から
16mm「内側」にあり、**一度も画面に出ていなかった**。外耳の前面を
縁が立って中央が沈む椀（`_ear_front`）にして、そこに28mm浮かせて座らせる。
ウサギと同じで、浮かせ量は輪郭線の `A.ink`（0.022）を超える必要がある。

### 帽子とヘルメット（全種共通）

`cap_crown` と `cap_bill` を焼いてある。**哺乳類・カエル・ペンギン・
カブトムシが同じ2つを使う**（鮭は `noHat`）。ヘルメットは同じ殻を
一回り大きくし、ツバを `z` 方向に潰して使う。触ると全種に効く。

- 半球と箱では**正面から眉の上の黒い帯、横から板に板**にしかならない。
  クラウンはハチマキの位置に段、パネルの折り返しに肩を持たせる。
- **ツバは二重に曲げる。** 長さ方向に垂れ、幅方向に反る。反りがないと
  カメラがツバの面に近づいた瞬間に消える——野球選手ではそれが大半の時間。
- **ツバは輪郭線に2回食われる。** 法線方向に `A.ink` だけ膨らむパスが
  上面と下面の両方から来るので、**厚みは `A.ink` の2倍以上**いる。
  現実的な34mmで焼いたら目の上の真っ黒な帯になった。いまは58mm。
- **ツバを垂らしすぎると目が隠れる。** ゲームのカメラはたいてい頭より下に
  あるので、垂れは44mmに抑え、根元はハチマキの段から出している。
  **目が見えないクマはかわいくない。**
- **`capY` は帽子の形に紐づく。** ウサギの `-0.075` は平たいドーム時代の値で、
  新しいクラウンでは目にツバがかかった。帽子を作り直したら全種見直すこと。
- 造形ラボの**セット `f`** が帽子とヘルメットを正面・3/4で出す。
  **カメラを頭より下に置いてある**のは、ツバが目にかかるのを捕まえるため。

**ハマった点** ツバを `oriented()` に通し忘れて裏返しに巻けた。塗りが
カリングされ、輪郭シェルだけが残って**真っ黒な帯**になる。
閉じた立体は必ず `oriented()` を通すこと。

### ウサギの耳と下半身（段階3）

ウサギは**頭と胴を共有リグ／`mesh:` 経路に残したまま**、耳・腿・後ろ足・
尻尾だけを焼いてある。名前は `rabbit_ear` `_earin` `_thigh` `_foot` `_tail`。

- **耳を頭メッシュに含めてはいけない。** 熊の丸耳は頭蓋の一部でよいが、
  ウサギの耳はシルエットの半分を占め、**走ると後ろに倒れ**（`p.legL/legR`
  から傾きを作っている）、内側は別の色。頭と一体にすると動かせなくなる。
  ウサギを `mesh:` 経路に載せるときも、耳は独立したパーツのままにすること。
- 耳の断面は**レンズを前に反らせたもの**（`cupped()`）。`cup` が縁を +z 側へ
  起こし、中央を沈める。これが「耳」と「板」の差そのもの。
- **内側のピンクは外耳の表面から 0.030 出す。** 面一にすると輪郭線パスに
  飲まれて消える。ただし縁が `cup` で 0.05 ほど前に出ているので、
  出しても「耳の内側に収まっている」ように見える。
- 腿・脛・足は**共有リグと同じ股関節**（`y+0.44`, `±0.155`）から吊る。
  走塁・スライディング・転倒はリグ側にあるので、ここを動かすと全部ずれる。
- 足は**踝を原点に +z へ伸ばして原寸で焼く**。`limb()` が返す踝の位置に
  同じ `rx`/`rz` で置けば、脚の振りにそのまま乗る。

**ハマった点** `lerp_profile()` は**キーが昇順**であることを前提にしている。
腿のプロファイルを「上から下」（降順）で書いたら、全リングが先頭の値を返して
**腿が鉛筆のように細くなった**。エラーは出ない。断面テーブルは必ず昇順で書く。

**巻き順は `oriented()` に任せる。** 閉じた立体なら符号付き体積を見て
裏返っていれば反転する。巻きを間違えた面は**見えないだけでエラーも出ない**ので、
リングの向きを人間が考えるのは割に合わない。

### 顔はテクスチャ

- 目・眉・鼻・口・ひげは**キャンバス2Dで描いてテクスチャに焼いている**（`buildFaceAtlas`）。
  1024x1024を128pxで8x8に区切り、**行=動物、列0-3=頭（目・眉）、列4-7=マズル（鼻・口）**。
  列がそのまま表情（`EXPR` = idle / focus / happy / down）。起動時に1回だけ生成する。
- 貼り付け先は `facep`（球）／`facepr`（rbox）という**その頭と同じ曲面のパッチ**。
  頭と同じ数値でスケールすれば表面にぴったり乗る。球のパッチをrboxの頭に貼ると
  中心から離れるほど約0.02m内側に沈んで顔が埋まるので、頭の形で使い分けること。
- 例外が2つある。**ウサギ**はマズルがほぼ平らなので鼻と口も頭のセルに描く（`onHead`）。
  **ペンギン**は白い顔パッチが頭より前に出ていて目を隠すので、顔パッチの球に貼る（`onPatch`）。
- デカールは**輪郭線パスでは描かない**（`R.inkW === 0` のときだけ）。絵自体が線画なので
  輪郭は要らないし、描くと真っ黒な板になる。
- 表情は `pose.face`。`G.faceBat` / `G.faceFld` を `finishAtBat` で決め、
  **`result` フェーズの間だけ**顔に出す。それ以外のとき打者と投手は `focus`。
- 手描きの線画を焼きたいときは `tools/face-lab.html`（`sh tools/build-lab.sh` で生成）。
  スキャンを読ませて紙を抜き、位置を合わせ、3Dで確認してから貼り付け用の1行を出す。
  その1行を `FACE_SCANS` に入れると、そのセルだけコードで描いた顔と差し替わる。
  ツールはゲームと同じ `10-core.js` / `30-actors.js` を使うので見た目が一致する。

### 結果表示はプレーの瞬間に合わせる

- バナーと効果音は打った瞬間ではなく、**その言葉が指す瞬間**に出す。
  `finishAtBat` が `G.pending = {text, big, snd, at}` を積み、`play` フェーズの
  更新で `at` に達したら `flushResult()` が鳴らす。`setPhase` は `play` から
  出るときに必ずフラッシュするので、言い忘れは起きない。
- `at` は `resultDelay()` が決める。基準時計は**プレースクリプトと同じ `G.flightT`**
  （`G.flight` がないときだけ `G.pt`）。エラーは捕り損ねた瞬間、フライは捕球、
  ゴロやゲッツーは送球が到達した時刻、安打は打球が処理された時刻。
- `applyOutcome` の中で `Snd.*()` を直接呼ばないこと。`o.snd = 'mitt'` のように
  名前を置いて、バナーと一緒に鳴らす。

### フライまわり

- **ファウルフライ／キャッチャーフライ**は `foulCatch()`。打ち上げ角32度以上・
  滞空1.5秒以上・落下点30m以内が条件で、二塁手・遊撃手・中堅手は追わない。
  捕球確率は66%（画面際で体勢が悪いため）。外れたらただのファウルに戻る。
  フェアの高いポップは通常の守備判定でキャッチャーが処理し、
  そのまま「キャッチャーフライ」の実況になる。
- **外野手はピッチャーに直接返球しない**（`relayIn`）。打球の落ちた線上、
  ホームから `dist*0.55`（20〜40m）の位置にカットマン（レフト方向ならSS、
  ライト方向なら2B）が入る。
- **止まっている走者の離塁**は `leadOff(i)`。滞空時間が長いほど深く出る。
  高いフライなら一塁走者は13.6m（二塁までの中間は13.7m）まで進んで待ち、
  捕球後0.85秒でベースに戻る。ライナー（滞空1.6秒未満）は一歩も出ない。
- **犠牲フライのバナーは走者がホームを踏んだ瞬間**に出す。`resultDelay` が
  ホームに向かうムーバーの `dur - t` を見ている（ムーバーの時計と
  `G.flightT` は原点が同じ）。

### 3アウトチェンジ

- `G.outs >= 3` で即 `endHalfInning()` せず、**`change` フェーズ（1.9秒）**を挟む。
  「スリーアウト／チェンジ」の大バナー、カメラは俯瞰、守備は `DUGOUT` に走る。
  打者もこの間は描かない。終わってから `endHalfInning()`。

### 走塁の見せ方

- `styleBaseRunning(o)` が各走者に付ける。`moveRunner` はムーバーを返す。
- **打者走者は1塁で止まらない。** 内野の打球なら `over = {ret:true}` で
  ベースを5.4m駆け抜けて戻る。外野に抜けてセーフなら `{ret:false}` で
  2塁方向に3.6m回り込んで止まる。**フライアウトのときは付けない**（引き返すので）。
- **送球が来る塁に入る走者はスライディングする**（`mv.slide`）。判定は
  `play.throwTo` / `play.via` が向かう塁と5m以内かどうか。2塁・3塁・本塁のみ。
- **2アウトならフライでも走者は走り出す**。捕球でイニングが終わるので進塁は
  ルール上無意味だが、止まって見ているのは不自然なので `retired` の
  ムーバーとして走らせる。
- ムーバーは**フェーズに関係なく毎フレーム進む**（`update` の走者ループは
  switch の外）。だから駆け抜けは `play` を延長しなくても `result` 以降で
  最後まで見える。`playLength` を伸ばすとゴロのたびに試合が1.5秒遅くなる。

### 判定と表示は同一のソースから

- `fieldBall()` が返す **`play`（プレースクリプト）が唯一の真実**。
  `{ fidx, cutIdx, cutT, pt, air, moves[], throwTo, throwDur, via, viaDur, fumble, fumbleTo }`
- アニメーションはこれを再生するだけ。独自に物理を進めてはいけない。
- これを分離していた頃、「ピッチャーゴロ」の実況でボールがセンターまで転がっていた。
- **新しい打球結果を追加するときは、必ず `play` を埋める**こと。

---

## ゲームモデル

**フェーズ** `ready → aim（人が投げる場合）→ pitch → play → result → ready`
（ほかに `halfend` / `walkoff` / `gameover`）

**モード** `G.mode` = `'cpu'`（対CPU）/ `'vs'`（2人対戦）/ `'auto'`（CPU同士・検証用）
`humanBats()` / `humanPitches()` で分岐する。`userBatting()` を直接使わない。

**守備判定** 打球を物理シミュレーション → 各野手が実際にグラブを届かせられる
最初の時刻を求める（`interceptOn`）→ 送球到達と走者到達を競走させて結果を出す。
誰も追いつけない場合は**停止地点に最も近い野手**が回収する（先頭要素＝投手が
選ばれる同着バグがあった）。

**主要な定数**（`src/40-game.js`）

| | 値 | 意味 |
|---|---:|---|
| `SWING_LAG` | 0.09 | スペース押下からバットがゾーンに入るまで。スイングのキーフレームと一致必須 |
| `TIME_WIN` | 0.115 | タイミング許容幅（秒） |
| `MEET_WIN` | 0.46 | ミートカーソルの許容幅（m） |
| `BAT_X, BAT_Z` | 0.95, 0.25 | 右打者の立ち位置（+x＝三塁側） |
| `LEAD_OFF` | 2.6 | 走者のリード幅 |
| `FUMBLE_T` | 0.66 | エラー時にボールがこぼれている時間 |

---

## 球場

| id | 重力 | フェンス(中堅/両翼) | 天井 | 守備速度 | 特殊ルール |
|---|---:|---:|---:|---:|---|
| `dome` | 9.8 | 104 / 86 | – | 1.00 | 標準 |
| `road` | 9.8 | 88 / 57 | – | 0.94 | 車が場内を走る。打球は跳ね、選手は轢かれる。轢かれた走者はアウト |
| `market` | 9.8 | 62 / 48 | 10.5 | 0.84 | 天井直撃は落下。買い物客と衝突。本塁打は低いライナーのみ |
| `river` | 9.8 | 112 / 96 | – | 1.00 | フェンスなし |
| `moon` | 5.4 | 176 / 146 | – | 0.90 | 低重力。滞空時間が伸びる |

`road` / `market` は `lanes` で移動体を定義する（`walk: 1` で買い物客）。
移動体は `carsSnapshot()` で打球シミュレーションにも渡され、**判定と表示が一致する**。

---

## 検証のしかた

CPU同士の自動対戦をヘッドレスで回す。ブラウザのコンソールで実行する。
**出力は失敗項目だけに絞る**こと（大きなJSONを返すとコンテキストを浪費する）。

```js
const bad = []; G.mode = 'auto';
for (const N of [3, 6, 9]) { G.innings = N;
  for (const s of ['dome','road','market','river','moon']) {
    startGame(s, 'bears', 'rabbits'); let g = 0;
    while (!G.over && g < 900000) { g++; update(1/60);
      if (!isFinite(G.ball.x) || G.outs > 3 || G.score[0] < 0) { bad.push({N,s,why:'state'}); break; } }
    const sum = G.lines.map(L => L.reduce((x,y) => x + (y||0), 0));
    if (g >= 900000) bad.push({N,s,why:'hang'});
    if (sum[0] !== G.score[0] || sum[1] !== G.score[1]) bad.push({N,s,why:'linescore'});
    const occ = G.bases.filter(Boolean);
    if (new Set(occ).size !== occ.length) bad.push({N,s,why:'dup runner'});
  } }
G.innings = 6; G.mode = 'cpu'; G.active = false;
({ ok: bad.length === 0, bad });
```

**必ず見る項目** ハングしない／NaNが出ない／アウトが3を超えない／得点が負にならない／
ラインスコアの合計がR欄と一致する／同じ走者が2つの塁に重複しない。

**成績の較正目標**（9イニング換算・1チームあたり。CPU同士で測る）

打率 .24 前後 ／ 安打 8〜8.5 ／ 本塁打 1.1 ／ 三振 8〜9 ／ 四球 3〜4 ／ 得点 4.5 ／
1打席の球数 3.5〜3.9 ／ ストライク率 50%前後

打撃系のパラメータを触ったら必ず測り直す。**打ち損じがファウルになる確率**
（`50-main.js` の `doContact`）が打席の長さを支配していて、ここを動かすと
三振と四球が同時に壊れる。

**ブラウザが手元にないとき** `node tools/shots.js --check` が全種 × 全ポーズの
glError を、`node tools/shots.js bear acdf out/` が接触シートのPNGを出す。
`--game out/` は本体を自動対戦で進めて撮る。Chromium をヘッドレスで叩くだけで、
見えるものはラボと同じ。**造形は見ないと直せない。**

**造形を見るとき** `sh tools/build-shape.sh` → `tools/shape-lab.html`。
1体を4方向・接触シートで描く。`LAB('bear', 'c')` でコンソールから切り替わる
（a=全身4方向 / b=真上と走り / c=顔の寄り / d=打席・万歳・転倒・帽子なし /
e=カブトムシの角と前脚 / f=帽子とヘルメット）。
ゲームと同じレンダラを使うので、ここで見えるものがそのまま出る。

**描画負荷** 1フレーム 3〜5ms（全キャラに輪郭線パスがあるので描画数は約2倍）。
**1回の計測で判断しないこと。** 熊のメッシュ化で1回ずつ測って「22%遅くなった」
と読んだが、4回ずつ測ると前後で差はなかった。打率のA/Bとまったく同じ罠。

**生成物のサイズ** `animal-baseball.html` が **512KB を超えると
プレビューペインで開けなくなる**（現在 493.0 KiB）。メッシュを足すときは
分割数を削ってでもここに収めると、検証が楽になる。

---

## 作業上の約束

- 修正は必ず `src/` に対して行い、`animal-baseball.html` を直接編集しない。
- 公開は `Artifact` ツールで `animal-baseball.html` を同じパスに再公開する（URLが維持される）。
- 並行セッションで作業するときは `src/50-main.js`（最大・全機能が集まる）を
  2人で同時に触らない。球場追加は `20-world.js`、キャラは `30-actors.js` で独立して進む。
- **区切りのついた変更は毎回コミットする**（都度たずねない）。pushは指示があったときだけ。
- キャラの造形は参考イラストがある。細かい造形を足すときは、輪郭線パス
  （`R.inkW > 0`）で描くかどうかを必ず決める。輪郭は法線方向に `A.ink` だけ
  膨らませるので、パーツがその太さより小さいと塗りつぶされる。
