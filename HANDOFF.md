# 引き継ぎ書 — どうぶつベースボール

最終更新 2026-09-22 ／ 対象コミット `HEAD`

この文書は**いまどこまで進んでいて、次に何をするか**を書いたもの。
コードを触る前に読むべき**不変条件・地雷・検証手順**は [CLAUDE.md](CLAUDE.md) にある。
役割が違うので、どちらも消さないこと。

- **CLAUDE.md** … 触る前に必ず読む規則。過去に間違えて時間を溶かしたものの集積。
- **HANDOFF.md**（これ） … 現在地と残作業。進んだら書き換える。
- **README.md** … 外から見た紹介とビルド方法。

---

## 1. 現在地

外部ライブラリなしの単一HTMLで動く3D野球ゲーム。WebGL 1.0を自前で叩いている。
**遊べる状態で、既知の破綻はない。**

| | |
|---|---|
| ブランチ | `claude/overall-brushup-78nu8g`（`claude/bear-form-gfx2ge` の上に積んである） |
| 最新コミット | 下の「これまでの流れ」15 を参照 |
| push | **済**（2026-09-22、ドラフトPRとして main に出してある） |
| 公開URL | https://claude.ai/code/artifact/98937406-1626-4b82-b5ce-d3c0bcaf218d |
| 作業ツリー | きれい |
| 生成物 | 約478,500 バイト＝**467 KiB**（512KiB まで45KB） |

**pushは指示があったときだけ**という約束は変わらないので、
引き継ぎ先が別のマシンなら、作業前に未pushのコミットが溜まっていないか確認すること。

### 動くもの

- 6チーム（クマ・ウサギ・サケ・カエル・ペンギン・カブトムシ）× 5球場
- 対CPU／2人対戦／CPU同士（検証用）
- 野球のルールは一通り。フライ、ファウルフライ、キャッチャーフライ、
  犠牲フライ、ゲッツー、エラー、中継返球、3アウトチェンジの演出まで。
  **盗塁・ボーク・暴投・振り逃げ・インフィールドフライ・送りバント／スクイズ・
  スリーバント失敗**も入った（2026-09-22）。サヨナラは勝ち越し点で止まる
- 顔はテクスチャ（4表情が試合の出来事に連動）。ウサギの目元だけ
  **お子さんの手描きの線画**をスキャンして焼き込んである
- クマ・サケ・カブトムシは球を積んだ造形ではなく、`tools/mesh2js.py` で
  **焼いたメッシュ**（段階3）。クマは共有リグを残したまま殻だけ焼いてあり、
  **残りの哺乳類も同じ手順で焼ける**
- **帽子とヘルメットも焼いたメッシュ**（`cap_crown` / `cap_bill`）。
  鮭以外の全種が同じ2つを使うので、触ると全種に効く

---

## 2. 最初の10分

```bash
cd C:\claude\game
sh build.sh                                     # src/ を連結して animal-baseball.html を生成
cat src/*.js > /tmp/all.js && node --check /tmp/all.js   # 構文チェック
node tools/sim.js                               # CPU同士45試合の完走チェック
```

`animal-baseball.html` をブラウザで開けば遊べる。サーバは要らない。

このPCには Python 3.13 と Node 24 が入っている（2026-08-29 導入）。
メッシュを焼き直すなら `python tools/mesh2js.py`。

**まず確認すること。** ブラウザのコンソールで:

```js
R.gl === false        // true なら起動に失敗している（後述の地雷1を疑う）
```

---

## 3. これまでの流れ

古い順。各行が1コミットではなく、ひとかたまりの作業。

1. **ゲーム一式** — レンダラ、球場、キャラ、ルール、走塁
2. **ウサギの作り直し** — お子さんの参考イラストに寄せた
3. **輪郭線をスクリーン空間に** — 遠近で太さが変わらない。分割数も上げた
4. **顔をテクスチャ化** — 表情4種を試合の出来事に連動させた
5. **手描き線画の取り込み** — `tools/face-lab.html` で紙を抜いて位置合わせし、
   アトラスの1セルだけ差し替える仕組み。ウサギに適用済み
6. **見せ方の作り込み6件** — 犠牲フライの表示タイミング、ライナー時の帰塁、
   高いフライでの離塁、ファウル／キャッチャーフライ、3アウトチェンジ、中継返球
7. **コンセプトの確定** — 「かわいいけど、ちょっと怖い」。CLAUDE.md 冒頭に全文
8. **猫と狐を外し、サケとカブトムシを投入** — ここで**メッシュ焼き込み（段階3）**を新設
9. **サケの挙動** — 立たせるのをやめ、横倒しでビチビチ跳ねる状態を通常に。
   打席では体ごと水平にすっ飛んでいってぶつかる。帽子は廃止
10. **熊を段階3に** — 頭とマズルが1つの面になり、胴に肩がついた。
    焼いたメッシュにUVを持たせられるようにしたので、**焼いた頭でも
    顔のアトラスが使える**。これで哺乳類のメッシュ化の道が開いた
11. **サケの赤い斑（`sal_blush`）が一度も描かれていなかったのを修正。**
    帯を1列に潰すバグで三角形が0枚だった。出したら体の大半を覆って
    しまったので、側線に沿う細い帯に直してある
12. **ウサギの耳・腿・後ろ足・尻尾を焼いた**（2026-09-12）。耳は前に反り、
    走ると後ろに倒れる。頭と胴はまだ共有リグのまま
13. **クマの積み残しを潰した**（2026-09-19）。耳が真横から板になっていたのと、
    **内耳が外耳の内側16mmにあって一度も画面に出ていなかった**のを直し、
    ついでに**帽子とヘルメットを焼き直した**——熊の頭がよくなった分、
    半球＋箱の帽子がいちばん弱くなっていたため。詳細は CLAUDE.md の
    「帽子とヘルメット（全種共通）」
14. **クマを丸め直した**（2026-09-20）。13で入れた実物寄りのマズルの段差と
    背中の隆起が**不自然**だという指摘。顔を戻し、胴をタマゴにし、
    首をなくし、足を短く太くした。耳と帽子は13のまま。
    **参照はぬいぐるみであって動物ではない**——CLAUDE.md 熊の節の
    「いちばん高くついた勘違い」に書いた

15. **全体の見直し**（2026-09-22）。
    - **走塁の穴を塞いだ。** 一塁の走者が進むとき三塁で待つ走者を上書きして
      **走者が1人消えていた**。2アウトからのゲッツーで3アウト目の回に得点が
      入っていた。サヨナラで全員還っていた。犠飛・犠打が打数に入っていた。
      エンタイトルツーベースで余計に進塁できた。打点の付き方も直した
    - **打球のないプレー**を足した：盗塁（盗塁死あり）、ボーク（鮭は静止
      できないので多い。条文つき）、暴投、振り逃げ、インフィールドフライ、
      送りバントとスクイズ（CPUも使う）、スリーバント失敗の三振
    - **カブトムシが角で打つ**ようになった。バントの構えも入った
    - チーム選択のカードを**ゲームの3Dモデルで描く**ようにした
    - 地面の芝・土・ラインの**z-fighting**（遠景のファウルゾーンの階段）を解消
    - `build.sh` がコメント行と字下げを落とすようになり、**約38KB軽く**なった
    - ヘッドレスの自動対戦ツール `tools/sim.js` を足した
    - **注意**：カブトムシの角打ちは一度 `claude/project-thread-kmxhdm` で
      作られたが、pushされないまま失われていたので、ここで作り直してある

16. **クマをオーナーの下絵に寄せた**（2026-09-23）。半目とニヤリ口、
    大きいマズル、無帽、クリーム地のピンストライプ、濃い肉球。
    「少し憎らしいが、愛らしい」を何が運んでいるかは CLAUDE.md 熊の節の
    「下絵（2026-09-23）」

猫と狐は**チームからは外したが `ANIMALS` には残っている**。
買い物客（`SHOPPER_KIND`）と顔アトラス（`FACE_ORDER`）が参照しているので、
消すなら両方から外すこと。パンダとカバも同様に、チームにはいないが生きている。

---

## 4. 残っていること

### 4-1. 捕食 — コンセプトの中核、未着手

**「怖い」はキャラクターが担当する。** クマがウサギを食べる、といった
現実の動物関係が野球場でそのまま発動する。**まだ実装していない。**
「先にキャラクターを磨き込む」という判断（2026-09-10）でこの位置にある。

実装に入る前に決めることは CLAUDE.md の冒頭に列挙してある（裁定・復帰の有無・
頻度・捕食関係の表）。**笑いの核心は裁定**——野球規則に条文がないので、
それらしい条文をでっち上げて審判が淡々と適用する、という形にする。

### 4-2. 未決：プレイヤーに走塁を操作させるか

いまの操作は**打撃と投球だけ**。ハザード（車・買い物客・将来の捕食）が
起きても見ているだけになる。捕食を入れるなら、ここが効いてくる。

### 4-3. 較正 — おおむね目標内

2026-09-22 に `node tools/sim.js --stats 150` を3回回した値で、
打率 .23 ／ 安打 7.9 ／ 本塁打 1.1 ／ **三振 8.4** ／ 四球 3.3 ／ 得点 4.7。
以前ここにあった「三振 10.2」は古い値で、見直しの前からすでに 8.3 前後だった。

残る宿題は**1打席の球数が 3.45 で目標（3.5〜3.9）をわずかに下回る**こと。
`50-main.js` の `doContact`（打ち損じがファウルになる確率）が打席の長さを
支配している。**ここを動かすと三振と四球が同時に壊れる**ので、必ず測り直すこと。

そして**20試合1本で判断しないこと。** 得点の試行間ばらつきが
1チームあたり 4.6〜6.2点ある。過去にこれで「ファウルフライが打率を
32ポイント下げた」と誤読し、正しい実装を弱めかけた。

### 4-4. 造形の残り

- ~~カブトムシの6本足が中央に寄る~~ → **解決**（2026-09-12）。付け根を
  ベイカーで体表から解き、基節の玉を足した。角・頭・脚をまとめて作り直してある
- ~~カブトムシの打撃に動きがない~~ → **解決**（2026-09-22）。`beetleSwing` で
  体ごと前胸の付け根を支点に振り、角で打つ
- ~~段階3のメッシュはサケとカブトムシだけ~~ → **熊も焼いた**（2026-09-12）。
  頭・マズル・耳・胴。**残りの哺乳類（カエル・ペンギン）は
  プリミティブのまま**で、熊と同じ手順でそのまま焼ける
  （CLAUDE.md「哺乳類をメッシュ化する」）。とくに**ピンストライプの箱**は
  どの哺乳類でも胴から飛び出していて、輪切りに焼くだけで直る
- **ウサギは耳・腿・後ろ足・尻尾だけ焼いてある**（2026-09-12）。
  頭と胴は手つかずなので、`mesh: 'rabbit'` で熊と同じように焼けば完成する。
  そのとき**耳を頭メッシュに含めないこと**（走ると倒れるので動かせなくなる）。
  CLAUDE.md「ウサギの耳と下半身（段階3）」に理由を書いた
- ~~帽子とヘルメットが板に見える~~ → **解決**（2026-09-19）。焼いた
  `cap_crown` / `cap_bill` に差し替えた。**全種共通なので触ると全種に効く**
  （CLAUDE.md「帽子とヘルメット（全種共通）」）
- **クマは 2026-09-20 に丸め直した。** 9/19版は実物のクマに寄せて
  マズルに段差、背中に隆起を入れたが、**どちらも不自然**だという指摘を受けて
  戻した。参照はぬいぐるみであって動物ではない（CLAUDE.md 熊の節の
  「いちばん高くついた勘違い」）。9/23 に下絵に合わせて無帽・半目になった
- 手描き線画は**ウサギの1セルだけ**。他の動物・他の表情も同じ手順で差し替えられる

### 4-5. 頂点数とファイルサイズの天井

インデックスが **Uint16 で1本の共有バッファ**なので、
**全ジオメトリ合計で 65,536 頂点が上限**。メッシュを足し続けると必ずぶつかる。
現在 12,914（コンソールで `GEO.pos.length / 3`）。まだ遠い。

**先に当たるのはファイルサイズのほう。** `animal-baseball.html` が 512KB を
超えるとプレビューペインで開けなくなり、**ブラウザでの検証ができなくなる**。
現在 467 KiB（残り45KB。`build.sh` がコメント行を落とすようになったため）。
詳しくは CLAUDE.md の「生成物のサイズ」。
細い帯に14リング、といった過剰な分割から削るのが安い——帽子の2メッシュは
鮭の側線の斑を46×9から24×7に落として相殺した。見た目は変わらない。

**カエルとペンギンを外す案**（クマ・ウサギ・カブトムシ・シャケの4チームに
する）はユーザーの許可済みだが、**まだ使っていない**。2種を `TEAMS` から
外しても造形コードは買い物客と顔アトラスが参照しているので残り、
容量はほとんど減らない。効くのは分割数を削るほう。

---

## 5. 特に踏みやすい地雷（3つだけ）

全リストは CLAUDE.md の「不変条件」にある。そのうち被害が大きい順に3つ。

**1. 後から連結されるファイルの値を、トップレベルで読んではいけない。**
`src/` は単純に `cat` で連結される。`30-actors.js` のトップレベルで
`40-game.js` の定数を読むと TDZ で例外になり、**スクリプト全体が止まって
画面が真っ黒になる。** 構文チェックは通る。関数の中でなら実行時評価なので問題ない。
実際にこれで一度、完全に起動しないビルドを作った。

**2. `animal-baseball.html` を直接編集しない。** `src/` の生成物。
1行変えれば全体が変わるので、並行ブランチでは必ず衝突する。
衝突したら解決せず再生成する（`.gitattributes` に手順あり）。
同様に `src/15-meshes.js` は `tools/mesh2js.py` の生成物なので手で触らない。

**3. 面の巻き順と、輪郭線に飲まれるパーツ。**
巻き方を間違えた面は**見えないだけでエラーも出ない**ので原因に辿りつきにくい。
また輪郭線は法線方向に `A.ink` だけ膨らませるので、
**それより小さいパーツは黒く塗りつぶされる**（ウサギの点目がこれで消えた）。

---

## 6. 検証のしかた

手順とコードは CLAUDE.md の「検証のしかた」にある。要点だけ:

- **`node tools/sim.js`** が CPU同士をヘッドレスで回す（`G.mode = 'auto'` +
  `update(1/60)` ループ）。`--stats 150` で成績、`--eval` で任意の確認
- **3・6・9回 × 5球場**を完走させ、ハング／NaN／アウト超過／
  ラインスコア不一致／走者の重複を見る
- 出力は**失敗項目だけ**に絞る（大きなJSONはコンテキストを食う）
- キャラを触ったら**全種 × 全ポーズ**を描いて GL エラーを見る。
  スイングを触ったなら**スイング後10秒経過**のポーズを必ず含めること
  （`G.batSwingT` はスイング後も増え続けるので、そこで固まる造形がある）

**ブラウザが手元にない環境では `tools/shots.js`**（2026-09-19 追加）。
Chromium をヘッドレスで叩いて造形ラボと本体を PNG で出す。

```bash
node tools/shots.js --check          # 全種 × 全セットの glError だけ報告
node tools/shots.js bear acdf out/   # 1体を指定セットで撮る
node tools/shots.js --game out/      # 本体を自動対戦で進めて撮る
```

**造形は見ないと直せない。** 今回クマを直せたのは、耳が真横から板に
なっているのと内耳が出ていないのを**画像で見た**からで、コードを
読んでいるだけでは両方とも気づけなかった。

---

## 7. 作業上の約束

- 修正は `src/` に対して行う。区切りがついたら**毎回コミットする**（都度たずねない）
- **push は指示があったときだけ**
- 公開は `Artifact` ツールで `animal-baseball.html` を**同じパスに**再公開する（URLが維持される）
- 並行セッションでは `src/50-main.js` を2人で同時に触らない。
  球場は `20-world.js`、キャラは `30-actors.js` で独立して進む
- 絵は最後までかわいいまま。怖いのは造形ではなく出来事
